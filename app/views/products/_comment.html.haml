%p
  %b=comment.user.try(:name)
.row
  .col-11
    %p=comment.body
    - if comment.created_at
      %p.muted.small="#{time_ago_in_words(comment.created_at)} ago"
  .col-1
    /= render partial: 'vote_button', locals: {vote_object: @product}
